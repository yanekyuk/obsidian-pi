// Provided by esbuild.config.mjs: every file under bin/ and pi-extension/, keyed by its path in the plugin folder.
declare module "bundled-files" {
	const files: Record<string, string>;
	export default files;
}
